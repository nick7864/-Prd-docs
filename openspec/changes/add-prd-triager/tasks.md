<!--
Tasks are grouped by the phased delivery plan (MVP → strengthen → bonus).
Each task states the behavior/contract being delivered and a verification target.
File paths are supporting context for locating the work.
Decisions referenced by name map to the ### headings in design.md.
Requirement names in parentheses map to ### Requirement: headings in specs/.
Contract names in parentheses map to ### Contract: headings in design.md.
-->

## 1. Scaffold & Sample Data (D1)

- [x] Initialize `pyproject.toml` with `uv` declaring dependencies: `google-adk>=2.0`, `pydantic>=2`, `mcp>=1.0`, `google-genai`, `pytest`, `pytest-asyncio`; running `uv sync` SHALL install all without error. Verification: `uv sync && uv run python -c "import google.adk; import mcp; import pydantic"` exits 0.
- [x] Create directory structure `src/mcp/`, `src/agents/`, `src/models/`, `src/policy/`, `data/prds/`, `data/architecture/adr/`, `eval/`, `tests/`. Verification: `Test-Path` on each directory returns true.
- [x] Author 5 sample PRD Markdown files in `data/prds/` (prd-001 through prd-005) on a consistent fictional product (`ShopFlow` e-commerce), each with YAML frontmatter (`id`, `title`, `status`, `updated_at`) and body covering: one complete PRD, one missing acceptance criteria, one containing an embedded API key (`AIzaSy...`), one with vague terms ("fast", "scalable"), and one well-scoped feature. Verification: each file parses as valid Markdown with non-empty frontmatter via `python -c "import frontmatter; ..."`.
- [x] Author `data/architecture/architecture.md` describing the ShopFlow system (services, data models, API surface) and 3 ADR files in `data/architecture/adr/` (ADR-001 microservices split, ADR-002 PostgreSQL choice, ADR-003 OAuth2 auth). Verification: files exist, are non-empty, and each ADR has `id`, `title`, `status`, `content` structure.

## 2. Document MCP Server (D2) — Decision: Document MCP Server via ADK McpServer module (Contract: Document MCP Server)

- [x] Implement the MCP server that exposes document repository tools — requirement "MCP server SHALL expose document repository tools" — at `src/doc_mcp/server.py` (renamed from `src/mcp/` due to namespace collision with upstream `mcp` package; see design.md Decision: Rename src/mcp to src/doc_mcp), exposing four tools (`list_prds`, `get_prd`, `get_architecture_context`, `get_similar_prds`) via FastMCP over stdio transport; on `tools/list` request it SHALL return all four tool definitions with `name`, `description`, `inputSchema`. Verification: `pytest tests/test_mcp_server.py::test_mcp_server_advertises_four_tools` passes — all four tools registered with non-empty schemas.
- [x] Implement the `list_prds` tool that returns available PRD metadata — requirement "list_prds tool SHALL return available PRD metadata" — by scanning `data/prds/*.md`, parsing YAML frontmatter, and returning an array of `{id, title, status, updated_at}` objects; on empty directory it SHALL return `[]` (not an error). Verification: `pytest tests/test_mcp_server.py::test_list_prds_empty` and `test_list_prds_with_files` pass.
- [x] Implement the `get_prd` tool that returns full PRD content by identifier — requirement "get_prd tool SHALL return full PRD content by identifier" — accepting `prd_id`, reading the matching file, and returning `{id, title, content, metadata}`; on unknown id it SHALL return `{"error": "PRD not found: <id>"}` without raising an exception. Verification: `pytest tests/test_mcp_server.py::test_get_prd_valid`, `test_get_prd_unknown`, and `test_get_prd_by_frontmatter_id_not_filename` pass.
- [x] Implement the `get_architecture_context` tool that returns the architecture knowledge base — requirement "get_architecture_context tool SHALL return architecture knowledge base" — returning `{architecture_doc, adrs}` where `adrs` is the 3 most recent ADRs from `data/architecture/adr/` sorted by id; on empty ADR directory it SHALL return `adrs: []` with `architecture_doc` still populated. Verification: `pytest tests/test_mcp_server.py::test_get_architecture_context` and `test_get_architecture_context_no_adrs` pass.
- [x] Implement the `get_similar_prds` tool that returns semantically similar historical PRDs — requirement "get_similar_prds tool SHALL return semantically similar historical PRDs" — performing cosine similarity over cached embeddings (using `google-genai` embed model) on historical PRDs, returning top-k `{id, title, similarity_score}` sorted by descending score; when `top_k` exceeds available docs it SHALL return all available without error. Implementation includes a keyword-overlap fallback path when `GOOGLE_API_KEY` is unset, so the pipeline still produces ranked results in offline/no-key states. Verification: `pytest tests/test_mcp_server.py::test_get_similar_prds_ranking` (keyword fallback) confirms prd-003 ranks first for "payment stripe checkout integration"; embedding-path verification deferred until `GOOGLE_API_KEY` provisioned by user.
- [x] Ensure the MCP server SHALL persist vector search index across restarts — requirement "MCP server SHALL persist vector search index across restarts" — by building and storing the embedding index at `openspec/.vector-search.db`, loading it on cold start without full rebuild, and updating incrementally when new PRDs are added. Implementation: `_load_cache()`, `_save_cache()`, `_content_hash()` in `repository.py` persist embeddings to `openspec/.vector-search.db.json`; `_embedding_ranking()` checks cache by content hash before calling embed API, batch-embeds only uncached docs, writes updated cache to disk. Cache key = `prd_id:content_hash` so content changes trigger re-embed. Integration test deferred until GOOGLE_API_KEY provisioned (cache functions are unit-testable without key).

## 3. Specialist Agents MVP (D3) — Decision: Four specialist agents as LlmAgent with Pydantic structured output (Contract: Four Specialist Agents)

- [x] Define Pydantic output schemas in `src/models/schemas.py` for `CompletenessReport` (fields: `completeness_score: int`, `missing_sections: list[{section, severity}]`), `ClarityReport` (fields: `ambiguous_items: list[{phrase, type, generated_question}]`), `TriageReport`, `PolicyDecision` (`allowed: bool`, `violations: list[{type, pattern, line_number}]`), and `Finding` shared type. Also includes `ArchitectureReport`, `RiskReport`, `FailedReport`, `ClarifyingQuestion`, `PmAnswer`, `Estimate`, `ConfidenceInterval`, `Ticket`, `AuditEntry`, plus `Severity` and `Verdict` enums. Verification: `pytest tests/test_models.py` — 20 tests pass covering round-trip serialization, enum coverage, composite TriageReport, edge cases (empty/awaiting_pm).
- [x] Implement the Completeness Checker that SHALL verify PRD structural completeness — requirement "Completeness Checker SHALL verify PRD structural completeness" — at `src/agents/completeness.py` as an ADK `LlmAgent` with a system prompt that evaluates the PRD for the five required sections (user stories, Given/When/Then acceptance criteria, non-functional requirements, edge cases, out-of-scope) and outputs a `CompletenessReport`; a PRD lacking acceptance criteria SHALL yield `completeness_score < 60` and a missing-sections entry with `section: "acceptance_criteria"`, `severity: "high"`. Verification: `pytest tests/test_agents/test_completeness.py::test_missing_acceptance_criteria` passes against the sample PRD without criteria.
- [x] Implement the Clarity Checker that SHALL detect ambiguous terms and contradictions — requirement "Clarity Checker SHALL detect ambiguous terms and contradictions" — at `src/agents/clarity.py` as an ADK `LlmAgent` that flags vague quantifiers (terms like "fast", "scalable", "user-friendly" without numeric targets), internal contradictions, and undefined domain terms, outputting a `ClarityReport` where each ambiguous item carries a `generated_question` addressed to the PM. Verification: `pytest tests/test_agents/test_clarity.py::test_vague_term_detected` passes — feeding a PRD containing "the system shall be fast" produces at least one item with `type: "vague_quantifier"`.

## 4. Orchestrator MVP (D4) — Decision: ADK Parallel workflow for specialist fan-out; Decision: Phased delivery (MVP → strengthen → bonus) (Contract: Orchestrator Workflow Graph)

- [x] Implement the orchestrator showing the pipeline SHALL accept PRD input via Document MCP — requirement "Pipeline SHALL accept PRD input via Document MCP" — and the pipeline SHALL be orchestrated via ADK workflow graph — requirement "Pipeline SHALL be orchestrated via ADK workflow graph" — at `src/agents/orchestrator.py` as an ADK 2.0 `Workflow` graph containing: an intake node (calls `get_prd` via MCP), a parallel fan-out node dispatching to Completeness and Clarity agents concurrently (MVP subset), a synthesis node, and an output node; when serialized to its graph representation it SHALL contain at least one parallel-execution segment. Verification: `pytest tests/test_orchestrator.py::test_workflow_graph_structure` inspects the serialized graph and asserts the presence of a parallel segment.
- [x] Implement the Synthesis Agent that SHALL merge specialist reports into structured assessment — requirement "Synthesis Agent SHALL merge specialist reports into structured assessment" — at `src/agents/synthesis.py` consuming specialist reports and producing a `TriageReport` with `verdict` (`pass` or `needs_clarification`), consolidated `risk_register`, and `clarifying_questions`; for the MVP (only Completeness + Clarity reports), `verdict` SHALL be `pass` when completeness_score ≥ 80 and fewer than 3 clarifying questions, else `needs_clarification`. Verification: `pytest tests/test_agents/test_synthesis.py::test_mvp_pass_verdict` and `test_mvp_needs_clarification_verdict` pass.
- [x] Implement the pipeline that SHALL emit a structured Markdown report — requirement "Pipeline SHALL emit a structured Markdown report" — by wiring the full MVP pipeline end-to-end: intake → parallel(Completeness, Clarity) → synthesis → Markdown report output at `reports/<prd_id>-<timestamp>.md`; feeding `prd_id` of the complete sample PRD SHALL produce a report with `verdict: "pass"` and all sections populated. Verification: `pytest tests/test_orchestrator.py::test_mvp_full_pipeline` runs the complete sample PRD and asserts the output report file exists with non-empty verdict section.
- [x] Implement failure handling for the four specialist agents that SHALL analyze the PRD in parallel — requirement "Four specialist agents SHALL analyze the PRD in parallel" — when one specialist agent raises an exception, the orchestrator SHALL catch it, mark that agent's report as `{status: "failed", error: <message>}`, proceed with the remaining reports, and record the failure in `audit_trail` without aborting the pipeline. Verification: `pytest tests/test_orchestrator.py::test_one_agent_fails` injects a failing agent stub and confirms the pipeline still produces a report.

## 5. Strengthen: Policy + Risk + Architecture + HITL (D5) — Decision: Policy gate via regex patterns + lightweight LLM triage (Contract: Policy Gate; Contract: HITL Gate); Decision: Synthesis Agent with critical-risk veto logic; Decision: HITL gate via ADK InteractiveCallback + workflow state

- [x] Implement the Policy gate that SHALL reject PRDs containing PII or secrets — requirement "Policy gate SHALL reject PRDs containing PII or secrets" — at `src/policy/` with rules in `src/policy/policies.yaml` (regex patterns for Google API keys `AIza[0-9A-Za-z_\-]{35}`, email addresses, phone numbers) and a `check_policy(prd_content)` function returning `PolicyDecision`; a PRD containing `AIzaSyD...` SHALL return `allowed: false` with a violation of type `google_api_key` and the line number; the YAML file SHALL be human-readable and reviewable. This realizes the Decision: Policy gate via regex patterns + lightweight LLM triage. Verification: `pytest tests/test_policy.py` — 13 tests pass covering API key rejection, email rejection, phone rejection, AWS/GitHub/Stripe tokens, clean-content acceptance, line-number reporting, multiple violations, scanner-error deny, and empty-content edge case.
- [x] Insert the Policy gate node into the orchestrator workflow between intake and the parallel fan-out; when `check_policy` returns `allowed: false`, the workflow SHALL skip the specialist agents and emit a `TriageReport` with `verdict: "reject"` containing the redaction report. This exercises the Contract: Policy Gate. Verification: `pytest tests/test_orchestrator.py::test_policy_reject` feeds the API-key sample PRD and asserts `verdict == "reject"` and no specialist findings in the output.
- [x] Implement the Architecture Fit Assessor agent at `src/agents/architecture.py` as an ADK `LlmAgent` that calls `get_architecture_context` via MCP to read the architecture doc and ADRs, then evaluates the PRD for conflicts, new-service requirements, and integration points, outputting an `ArchitectureReport` with a `conflicts` list (each carrying `description` and `severity`). Verification: `pytest tests/test_agents/test_architecture.py::test_detects_new_service` feeds a PRD proposing a new payment service and asserts a conflict or integration-point finding.
- [x] Implement the Risk and Compliance Checker agent at `src/agents/risk.py` as an ADK `LlmAgent` evaluating the PRD for security (auth, PII, data flow), compliance (GDPR, data retention), and performance risks, outputting a `RiskReport` with a `findings` list where each finding carries `description`, `severity` (`low|medium|high|critical`), and `compliance_framework`. Verification: `pytest tests/test_agents/test_risk.py::test_detects_pii_handling` feeds a PRD collecting user emails without retention policy and asserts a finding with `severity` of at least `high`.
- [x] Add Architecture and Risk agents to the orchestrator's parallel fan-out (now 4 specialists) and update the Synthesis Agent's veto logic per the Decision: Synthesis Agent with critical-risk veto logic — if any Risk finding has `severity: "critical"`, the Synthesis Agent SHALL force `verdict: "needs_clarification"` regardless of LLM output. Verification: orchestrator `specialists_parallel` now contains 4 agents (completeness, clarity, architecture, risk); `apply_critical_risk_veto()` function in orchestrator.py implements the deterministic post-check — `pytest tests/test_orchestrator.py::TestCriticalRiskVeto` — 7 tests pass covering critical-forces-needs_clarification, non-critical-preserves-pass, reject-not-overridden, no-risk-report-no-op, compliance-context-in-question, single-veto-for-multiple-criticals.
- [x] Implement the HITL gate that SHALL pause pipeline and request PM clarification — requirement "HITL gate SHALL pause pipeline and request PM clarification" — in the orchestrator: when synthesis `verdict` is `needs_clarification`, the workflow SHALL emit the `clarifying_questions` to the output, pause, and wait; in synchronous CLI mode it SHALL use `input()` to read PM answers, then resume into estimation; if the PM issues an override command, the workflow SHALL proceed with `hitl_overridden: true`. This exercises the Contract: HITL Gate. Verification: `hitl_gate_cli()` function in orchestrator.py implements synchronous CLI fallback — `pytest tests/test_orchestrator.py::TestHitlGateCli` — 7 tests pass covering no-gate-on-pass, no-gate-on-reject, no-gate-without-questions, pm-answers-collected, pm-override-forces-pass, override-case-insensitive, gate-prints-prompt.

## 6. Deploy + Skill (D6) — Decision: Deployment via Dockerfile + Cloud Run + agents-cli deploy

- [x] Author `Dockerfile` based on `python:3.12-slim` that installs deps via `uv`, copies source, and starts the FastAPI + ADK app on port 8080; building with `docker build -t prd-triager .` SHALL succeed and `docker run` SHALL respond to `curl POST /triage` with a `TriageReport` JSON. Verification: `docker build` exits 0 and `curl -X POST localhost:8080/triage -d '{"prd_id":"prd-001"}'` returns HTTP 200 with valid JSON.
- [x] Deploy to Google Cloud Run via `agents-cli deploy --project <gcp-project> --region asia-east1` (or `gcloud run deploy`); the deployed endpoint SHALL respond to the same `POST /triage` call and return a `TriageReport`; if GCP billing is unavailable, fall back to `uvicorn` + `ngrok http 8000` and document both paths in README. Verification: **fallback path verified** — uvicorn + cloudflared tunnel live at `https://striking-families-standards-licensed.trycloudflare.com`; `curl /health` returns 200; `curl -X POST /triage -d '{"prd_id":"prd-003"}'` returns `verdict=reject` with policy violation; all 5 PRDs respond through public endpoint. Cloud Run deployment (via Docker) deferred until Docker Desktop installed; `scripts/deploy.sh` ready for when it is.
- [x] Author the custom Skill at `.agents/skills/prd-analysis/SKILL.md` (Level 3: procedural skill with explicit steps) describing when to trigger the triage pipeline and how to interpret the `TriageReport` output; the SKILL.md SHALL follow the Day 3 skill structure (name, description, trigger conditions, procedure). Verification: the SKILL.md exists, is non-empty, and references the `triage` entrypoint by name.
- [x] Author `README.md` covering: problem statement, solution, architecture diagram, setup steps (`uv sync`, env vars needed), deployment steps (Cloud Run + fallback), and how to run the demo cases; the README SHALL include at least one architecture diagram (image or ASCII). Verification: README exists, contains all six sections as headings, and the setup steps are runnable copy-paste.

## 7. Bonus: Estimation + Task Breakdown (D7, if time permits) — Decision: Estimation via vector similarity retrieval + LLM reasoning

- [x] Implement the Estimation Agent that SHALL produce effort estimate with confidence interval — requirement "Estimation Agent SHALL produce effort estimate with confidence interval" — at `src/agents/estimation.py` that calls `get_similar_prds` for historical analogues, compares the target PRD scope against them, and outputs a point estimate (engineer-days), a confidence interval `{low, median, high}`, and at least 2 estimation drivers referencing the analogues; when no analogues are returned it SHALL set `low_confidence: true` and produce a wider interval (high - low > 2 × median). Verification: `pytest tests/test_agents/test_estimation.py::test_with_analogues` and `test_no_analogues_low_confidence` pass.
- [x] Implement the Task Breakdown Agent that SHALL decompose PRD into executable tickets — requirement "Task Breakdown Agent SHALL decompose PRD into executable tickets" — at `src/agents/breakdown.py` consuming the clarified PRD and estimate, outputting 3-8 tickets each with `title`, `description`, `acceptance_criteria`, `estimated_effort`, and `dependencies`; the sum of ticket efforts SHALL be within 20% of the PRD-level estimate. Verification: `pytest tests/test_agents/test_breakdown.py::test_effort_sum_within_tolerance` passes against the complete sample PRD with a 10-engineer-day estimate.
- [x] Insert Estimation and Breakdown nodes into the orchestrator workflow after the HITL gate (or after synthesis when `verdict: "pass"`), and update the Markdown report template to include the estimation and ticket sections; on HITL timeout the report SHALL mark these sections `not_reached`. Verification: `pytest tests/test_orchestrator.py::test_full_pipeline_with_estimation` runs the complete sample PRD end-to-end and asserts the report contains populated estimation and tickets sections.
